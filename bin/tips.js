#!/usr/bin/env node

const chalk = require('chalk');
console.log(chalk.green('Usage: @mhh init to start a new project'));
console.log('generate a project from a remote template (legacy API, requires @mhh-init)');
console.log();
console.log(chalk.green('Options'));
console.log('-V', '--version', 'output the version number');
console.log('-H', '--help', 'output usage information');